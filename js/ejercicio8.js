//Tarea -8.- Escribe un programa que pida un número y diga si es divisible por 2

let num = parseInt(prompt("ingrese un numero"));
if (num % 2 === 0) {
    document.write("Si es divisible por 2");
}else {
    document.write("No es divisible por 2");
}